# FastAPI & Server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# Document Parsing
PyMuPDF==1.23.8  # PDF parsing (fitz)
python-docx==1.1.0  # DOCX parsing
openpyxl==3.1.2  # Excel parsing
mammoth==1.6.0  # DOCX to HTML/text (alternative)

# Text Processing & NLP
# Note: langchain 0.1.0 has conflicts, using only text-splitters which is what we need
langchain-text-splitters>=0.0.1  # Use >= to allow compatible versions
nltk==3.8.1
regex==2023.12.25

# OpenAI
openai==1.6.1

# Database
pymysql==1.1.0  # Pure Python MySQL client (no system library needed)
sqlalchemy==2.0.23
prisma==0.11.0  # If using Prisma Python client

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.25.2

# Logging
loguru==0.7.2

# Optional: Local embeddings (if not using OpenAI)
# sentence-transformers==2.2.2
# torch==2.1.0

